export const title = "Module 2"

export const description = ""

export const image = "/chapter_headers/serverless-elements.png"
import { Vimeo } from "../../components/Video"

# Auth

We're using [useAuth](https://github.com/Swizec/useAuth) together with Auth0 to add authentication to your app.

Swizec walks you through the basic setup and you add your config values.
