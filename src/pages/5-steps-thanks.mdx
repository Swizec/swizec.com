---
title: "You're all set, thanks"
description: "You just subscribed to 5 Action Steps to Senior Engineer, thanks"
hero: ../images/SeniorMindset-cover-3d-thumb.png
---

The first email from **5 Action Steps to Senior Engineer** will be in your inbox shortly. Thanks for signing up.

I hope these emails give you the first few steps towards a more fulfilling career in software engineering. If not, you know where to find me.

Here's a book you might enjoy 👇

[![Senior Engineer Mindset ebook](../images/SeniorMindset-cover-3d.png)](/senior-mindset/)

import {
  ContentUpgrades,
  ArticleListing,
} from "@swizec/gatsby-theme-course-platform"
import { graphql } from "gatsby"

export const pageQuery = graphql`
  query {
    allMdx(
      filter: {
        internal: { contentFilePath: { regex: "/blog/.+/" } }
        frontmatter: { categories: { regex: "/SeniorMindset|Mindset/gi" } }
      }
      sort: { frontmatter: { published: DESC } }
    ) {
      nodes {
        fields {
          slug
        }
        frontmatter {
          title
          description
          published
        }
      }
    }
  }
`

Or you can check out the latest in my articles on engineering careers and the mindset of a true senior engineer.

<>
  {props.data.allMdx.nodes.splice(0, 5).map((props, i) => (
    <ArticleListing {...props} key={i} />
  ))}
</>
