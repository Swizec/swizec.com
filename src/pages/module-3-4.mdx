export const title = "Module 3"

export const description = ""

export const image = "/chapter_headers/serverless-elements.png"
import { Vimeo } from "../components/Video"

# Provisional prod deploy

Here we deploy the frontend to production still using the `dev` stage of the backend. The goal is to make sure it works and see how our app behaves on a real domain.

We're using Zeit but you can achieve the same flow with Netlify.

<Vimeo id="394978218" />

## Verify it works

<Vimeo id="394978438" />
